#!/bin/bash

die() {
    echo >&2 '!!'" $*"
    exit 1
}

[ ! -z "$MOZCFG" ] || die "No moz config"
[ -d "$MOZPATH/$MOZCFG" ] || die "Not built"
[ -f "$MOZPATH/$MOZTREE/mach" ] || die "No mach in this tree"

(
  cd "$MOZPATH/$MOZTREE" && cleangtk py2e ./mach "$@"
)
