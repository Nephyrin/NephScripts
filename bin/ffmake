#!/bin/bash

die() {
    echo >&2 '!!'" $*"
    exit 1
}

[ ! -z "$MOZOBJ" ] || die "No moz config"
[ -d "$MOZPATH/$MOZOBJ" ] || die "No initial build"
py2e $MOZPATH/smartmake/smartmake.py "$MOZPATH/$MOZOBJ" "$@"
